# ANUBIS-SPARK v1.0.0 Build Environment

## Build Configuration
ANUBIS_SOFT_ONLY=on
ANUBIS_LIB_DIR=/opt/homebrew/lib

## Compiler
GNAT_VERSION=14.2.1
GPRBUILD_VERSION=24.0.1

## Dependencies (Homebrew on macOS)
liboqs=0.14.0
libsodium=1.0.20
openssl@3=3.x

## Proof Tools
gnatprove (SPARK Pro)
CVC5 (SMT solver)
Z3 (SMT solver)

## Build Commands
alr exec -- gprbuild -P anubis_spark.gpr

## Proof Commands
alr exec -- gnatprove -P anubis_spark.gpr \
  --mode=prove --level=4 --timeout=60 --report=all

## Verification
# Check static linking (macOS)
otool -L bin/anubis_main | grep -E "liboqs|libsodium"
# Should return empty (both statically linked)

otool -L bin/anubis_main | grep -E "libssl|libcrypto"
# Should show OpenSSL dylibs only
